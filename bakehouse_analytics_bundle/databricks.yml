# This is a Declarative Automation Bundle definition for bakehouse_analytics_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bakehouse_analytics_bundle
  uuid: 0fe91bed-1bd8-42f3-9224-ee59660b1381

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-8f368e67-9502.cloud.databricks.com
    variables:
      catalog: bakehouse_analytics_dev
      schema: rnd
  prod:
    mode: production
    workspace:
      host: https://dbc-8f368e67-9502.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sambuddha36@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sambuddha36@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: bakehouse_analytics_dev
      schema: rnd
    permissions:
      - user_name: sambuddha36@gmail.com
        level: CAN_MANAGE
